name: Weekly Sunday Report
on:
  schedule:
    - cron: '0 0 * * 0' # Midnight every Sunday
  workflow_dispatch: # Allows you to click a button in GitHub to test it immediately

jobs:
  trigger_php_report:
    runs-on: ubuntu-latest
    steps:
      - name: Call Sunday Report Script
        run: |
          curl "https://mysusuapp.com/cron_sunday_report.php?key=Susu_Secure_9922"
